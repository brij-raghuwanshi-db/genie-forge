# Genie-Meta MVP Dependencies
# Core dependencies
databricks-sdk>=0.20.0
pydantic>=2.0.0
PyYAML>=6.0
click>=8.0.0
rich>=13.0.0

# Development dependencies (install with: pip install -r requirements.txt -r requirements-dev.txt)
# See requirements-dev.txt for development tools
